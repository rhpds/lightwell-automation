apiVersion: v1
kind: Service
metadata:
  name: artifactory-postgresql
  namespace: {{ .Values.artifactory.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    name: artifactory-postgresql
